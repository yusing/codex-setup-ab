Add portable skill bundles with create, verify, and extract commands. Make them reproducible, preserve executable scripts, reject corrupt or unsafe archives, and never overwrite existing files. Keep everything local and cover it with tests.
